apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.name }}-sa
  namespace: organistation-ns
  annotations:
    azure.workload.identity/client-id: {{ .Values.global.userAssignedIdentityID }}
